{
  "name": "pulse-fitness",
  "version": "0.1.0",
  "private": true,
  "description": "AI fitness companion PWA",
  "scripts": {
    "migrate": "node scripts/apply-migrations.mjs",
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "lint": "npm --prefix apps/web run lint"
  },
  "devDependencies": {
    "pg": "^8.13.1"
  }
}
